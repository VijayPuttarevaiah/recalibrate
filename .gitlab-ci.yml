# This file is a template, and might need editing before it works on your project.
# This is a sample GitLab CI/CD configuration file that should run without any modifications.
# It demonstrates a basic 3 stage CI/CD pipeline. Instead of real tests or scripts,
# it uses echo commands to simulate the pipeline execution.
#
# A pipeline is composed of independent jobs that run scripts, grouped into stages.
# Stages run in sequential order, but jobs within stages run in parallel.
#
# For more information, see: https://docs.gitlab.com/ee/ci/yaml/index.html#stages
#
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml

stages:          # List of stages for jobs, and their order of execution
  - build
  - test
  # - quality
  - publish
  - deploy

build:
  stage: build
  image: python:3.11
  script:
    - python --version
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - echo "Dependencies installed successfully"

test:
  stage: test
  image: python:3.11
  script:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - pytest || echo "No tests found, skipping"

# quality:
#   stage: quality
#   image: openjdk:11
#   script:
#     - echo "Compiling the project"
#     - mkdir smells/
#     - java -jar $CI_PROJECT_DIR/.gitlab/DesigniteJava.jar -i $CI_PROJECT_DIR -o $CI_PROJECT_DIR/smells/ -d
#   artifacts:
#     paths:
#       - smells/
#       - $CI_PROJECT_DIR

publish:
  image: docker:latest
  stage: publish
  tags: 
    - Project2
  variables:
    # these values may need to be different if using TLS, k8s, etc.
    # You can alternatively set defaults in your runner config
    DOCKER_TLS_CERTDIR: ""
    DOCKER_HOST: "tcp://docker:2375"
  services:
    - docker:dind

  script:
    - pwd 
    - echo $SERVER_IP
    - docker --version
    - echo dckr_pat_F2R5GiaZ1E_2gd4IjJ4bAR4M8XE | docker login -u vijayputtarevaiah --password-stdin
    - docker build -t docker.io/vijayputtarevaiah/projectgroup2:$CI_COMMIT_SHORT_SHA .
    - docker push docker.io/vijayputtarevaiah/projectgroup2:$CI_COMMIT_SHORT_SHA
  only:
    - main


deploy:
  image: alpine:latest
  stage: deploy
  tags:
    - Project2
  script:
    # Set correct permissions on the key file (Variable type must be 'File')
    - chmod 400 $ID_RSA
    - apk update && apk add openssh-client
    # 1. Login using the token via STDIN
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "echo 'dckr_pat_F2R5GiaZ1E_2gd4IjJ4bAR4M8XE' | docker login -u vijayputtarevaiah --password-stdin"
    # 2. Pull the image
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker pull docker.io/vijayputtarevaiah/projectgroup2:$CI_COMMIT_SHORT_SHA"
    # 3. Remove old container (ignore error if it doesn't exist)
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker container rm -f my-app || true"
    # 4. Run the new container
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker run -d -p 8073:8073 --name my-app docker.io/vijayputtarevaiah/projectgroup2:$CI_COMMIT_SHORT_SHA"
  environment:
    name: production
    url: csci5308-vm2.research.cs.dal.ca
  only:
    - main