services:
  db:
    image: mysql:8.0
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_DATABASE: backend_db
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uuser", "-ppassword"]
      timeout: 20s
      retries: 10

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/config:/app/config
    environment:
      - DATABASE_URL=mysql+pymysql://user:password@db:3306/backend_db
<<<<<<< Updated upstream
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - SERPER_API_KEY=${SERPER_API_KEY}
=======
      - GOOGLE_API_KEY=AIzaSyBU9eb_vuRqUj8388gvCcYF3PFvfwsTlLM
      - OPENROUTER_API_KEY=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
>>>>>>> Stashed changes
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=development
    restart: always

volumes:
  mysql_data:
